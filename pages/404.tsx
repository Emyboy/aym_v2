import Link from 'next/link'
import React, { ReactElement } from 'react'
import PageNotFound from '../components/PageNotFound'

interface Props {

}

export default function _PageNotFound({ }: Props): ReactElement {
    return (
        <PageNotFound />
    )
}
